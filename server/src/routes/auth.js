import { Router } from "express";

// Public auth routes (Google OAuth login/callback, token refresh). Team 2 owned.
// No endpoints yet — this is the scaffolding-stage stub.
const router = Router();

export default router;
