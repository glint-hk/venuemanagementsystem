import { Routes } from "react-router-dom";

// Route table. Each team adds its <Route> entries here as pages land in
// src/pages/ — see README.md "Team ownership map" for who owns what.
export default function App() {
  return <Routes />;
}
